server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/denis-zhdanov/samples"
          searchPaths: "kotlin/spring/config-server/settings/{application}/{profile}"